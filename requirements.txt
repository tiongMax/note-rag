# Core LangChain and LCEL APIs
langchain==1.3.13
langchain-community==0.4.2
langchain-text-splitters==1.1.2

# Postgres vector store (PGVector is imported from langchain-postgres)
langchain-postgres==0.0.17
# Newest release allowed by langchain-postgres 0.0.17 (which requires <0.4).
pgvector==0.3.6
psycopg[binary]==3.3.4

# Google Gemini embeddings and chat model
langchain-google-genai==4.2.7
python-dotenv==1.2.2

# Local PDF document-loader dependency
pypdf==6.14.2
